{
  "name": "jobsim-root",
  "version": "1.0.0",
  "description": "Root configuration for JobSim",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend/backend && npm run dev",
    "dev": "npm run dev:frontend & npm run dev:backend",
    "install:all": "npm install && cd frontend && npm install && cd ../backend/backend && npm install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
